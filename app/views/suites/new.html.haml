=simple_form_for @suite, :url => project_suites_path(current_project) do |f|

  = f.input :name
  = f.input :suite_type, :as => :select, :collection => Suite::TYPES
  = f.input :command
  = f.input :branch
 
  = f.submit
